package exercises

/*
 1. MyList: replace all FunctionX calls with lambda
 2. Rewrite the "special" adder as an anonymous function
 */
object AnonymousFunctionExercise extends App {
  val superAdder = (x: Int) => (y: Int) => x + y
}
